global:
  scrape_interval: 15s  # How often to scrape targets

scrape_configs:
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['cadvisor:8080']  # Replace localhost with your host's IP if necessary