# Archived Jenkins Jobs

These legacy job definitions are retained for historical reference. This directory is outside the active `jobs/` tree,
so it is not copied into Jenkins during installation or updates.

The archive contains the retired JupyterHub, aggregate-resource, passthrough-resource, deprecated auth micro-app,
HPDS UI, and PSAMA upload jobs.
